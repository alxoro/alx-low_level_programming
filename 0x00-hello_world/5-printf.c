#!/bin/bash
#include <stdio.h>

/**
 *main - This funtion prints a sentence followed by new line
 *Description: This funtion prints a sentence followed by new line
 *@void: return type
 *Return: always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
	return (0);
}
