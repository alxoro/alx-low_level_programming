#include "main.h"

/**
 * _strlen - it return the length of the string
 * @s: input parameter
 * Description: uses pointer
 * Return: Always (0)
 */
int _strlen(char *s)
{
	int i;

	for (i == 0; s[i] != '\0'; ++i)
	{
		continue;
	}
	return (i);
}
