#include "main.h"

/**
 * _islower - it prints 1 or 0 depending on the input
 * @c: first parameter
 * Description: it prints all the lowercase letters
 * Return: always 0
 */
int _islower(int c)
{
	return (c <= 'a' && c <= 'z');
}
